#include <bits/stdc++.h>
using namespace std;

// ============================
// QUESTION 1(a) Selection Sort
// ============================

void selectionSort(int a[], int n) {
    for (int i = 0; i < n-1; i++) {
        int minIdx = i;
        for (int j = i+1; j < n; j++)
            if (a[j] < a[minIdx]) minIdx = j;
        swap(a[i], a[minIdx]);
    }
}

// ============================
// QUESTION 1(b) Insertion Sort
// ============================

void insertionSort(int a[], int n) {
    for (int i = 1; i < n; i++) {
        int key = a[i], j = i - 1;
        while (j >= 0 && a[j] > key) {
            a[j+1] = a[j];
            j--;
        }
        a[j+1] = key;
    }
}

// ============================
// QUESTION 1(c) Bubble Sort
// ============================

void bubbleSort(int a[], int n) {
    for (int i = 0; i < n-1; i++) {
        bool sw = false;
        for (int j = 0; j < n-1-i; j++) {
            if (a[j] > a[j+1]) {
                swap(a[j], a[j+1]);
                sw = true;
            }
        }
        if (!sw) break;
    }
}

// ============================
// QUESTION 1(d) Merge Sort
// ============================

void mergeArr(int a[], int l, int m, int r) {
    int n1 = m-l+1, n2 = r-m;
    vector<int> L(n1), R(n2);
    for (int i = 0; i < n1; i++) L[i] = a[l+i];
    for (int j = 0; j < n2; j++) R[j] = a[m+1+j];
    int i=0,j=0,k=l;
    while (i<n1 && j<n2) a[k++] = (L[i]<=R[j]) ? L[i++] : R[j++];
    while (i<n1) a[k++] = L[i++];
    while (j<n2) a[k++] = R[j++];
}

void mergeSort(int a[], int l, int r) {
    if (l >= r) return;
    int m = (l+r)/2;
    mergeSort(a,l,m);
    mergeSort(a,m+1,r);
    mergeArr(a,l,m,r);
}

// ============================
// QUESTION 1(e) Quick Sort
// ============================

int partitionQS(int a[], int l, int r) {
    int pivot = a[r];
    int i = l-1;
    for (int j = l; j < r; j++) {
        if (a[j] <= pivot) {
            i++;
            swap(a[i], a[j]);
        }
    }
    swap(a[i+1], a[r]);
    return i+1;
}

void quickSort(int a[], int l, int r) {
    if (l < r) {
        int p = partitionQS(a,l,r);
        quickSort(a,l,p-1);
        quickSort(a,p+1,r);
    }
}

// ============================
// QUESTION 2
// Improved selection sort (min & max)
// ============================

void minMaxSelectionSort(int a[], int n) {
    int start = 0, end = n-1;
    while (start < end) {
        int minIdx = start, maxIdx = end;
        for (int i = start; i <= end; i++) {
            if (a[i] < a[minIdx]) minIdx = i;
            if (a[i] > a[maxIdx]) maxIdx = i;
        }
        swap(a[start], a[minIdx]);
        if (maxIdx == start) maxIdx = minIdx;
        swap(a[end], a[maxIdx]);
        start++; end--;
    }
}

// (You can add counting sort / radix sort here as additional)

// ============================
// MAIN
// ============================

int main() {
    return 0;
}
